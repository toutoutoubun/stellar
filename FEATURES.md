# Stellar — 機能説明書

> **最終更新**: 2026-05-08  
> **バージョン**: 0.1.0  
> **対応 OS**: macOS / Windows / Linux  
> **Tauri コマンド総数**: 147

本ドキュメントは Stellar の全機能を詳細に記述したリファレンスです。各モジュールの画面構成、操作フロー、技術仕様、API コマンドを網羅しています。

---

## 目次

1. [アプリケーション概要](#1-アプリケーション概要)
2. [文献ライブラリ](#2-文献ライブラリ)
3. [PDF リーダー & ハイライト](#3-pdf-リーダー--ハイライト)
4. [ノート & エディタ](#4-ノート--エディタ)
5. [下書きモード（長文執筆）](#5-下書きモード長文執筆)
6. [グラフビュー（知識ネットワーク）](#6-グラフビュー知識ネットワーク)
7. [引用ネットワーク](#7-引用ネットワーク)
8. [質的分析モジュール](#8-質的分析モジュール)
9. [量的分析モジュール（Data Studio）](#9-量的分析モジュールdata-studio)
10. [エクスポート & パッケージ](#10-エクスポート--パッケージ)
11. [クラウドバックアップ](#11-クラウドバックアップ)
12. [ブラウザ連携](#12-ブラウザ連携)
13. [設定 & カスタマイズ](#13-設定--カスタマイズ)
14. [オンボーディング](#14-オンボーディング)
15. [全文検索](#15-全文検索)
16. [データベース設計](#16-データベース設計)
17. [セキュリティ仕様](#17-セキュリティ仕様)
18. [キーボードショートカット一覧](#18-キーボードショートカット一覧)

---

## 1. アプリケーション概要

### 設計思想

Stellar は以下の原則に基づいて設計されています。

| 原則 | 説明 |
|---|---|
| **ローカルファースト** | すべてのデータはユーザーの端末内の SQLite データベースに保存されます。インターネット接続なしで全機能が動作します |
| **ユーザー負担ゼロ** | アカウント登録、サブスクリプション、クラウドサービスへのログインは一切不要です |
| **AI 非依存** | AI や LLM を使用しません。自動要約・自動コーディングなどの機能は意図的に排除し、研究者自身の判断を尊重します |
| **人文・社会科学特化** | 一般的な文献管理ツールが対応しない史料批判、プロセストレーシング、フレーミング分析などの分析手法を組み込んでいます |
| **E2E 暗号化バックアップ** | クラウドバックアップはエンドツーエンド暗号化されます。サーバー側ではデータを復号できません |

### 画面構成

```
┌──────────────────────────────────────────────────┐
│ タイトルバー（カスタムウィンドウコントロール）       │
├─────┬────────────────────────────────────────────┤
│     │                                            │
│ サ  │              メインコンテンツ                │
│ イ  │                                            │
│ ド  │  ┌──────────────┬─────────────────────┐    │
│ バ  │  │ リスト/一覧   │ 詳細/エディタ        │    │
│ ー  │  │              │                     │    │
│     │  │              │                     │    │
│     │  └──────────────┴─────────────────────┘    │
├─────┴────────────────────────────────────────────┤
│ コンテキストパネル（論文詳細/バックリンク等）        │
└──────────────────────────────────────────────────┘
```

- **タイトルバー**: カスタムデコレーション（`decorations: false`）。ドラッグによるウィンドウ移動、最小化・最大化・閉じるボタン
- **サイドバー**: ライブラリ / ノート / グラフ / 質的分析 / 量的分析 / 設定 の 6 セクション。折りたたみ対応
- **メインコンテンツ**: 選択されたセクションに応じた画面を表示。スプリットビュー対応
- **コンテキストパネル**: 論文のメタデータ詳細、バックリンク一覧、ハイライト一覧などを状況に応じて表示

### 多言語対応

| ロケール | 言語 | ネイティブ名 |
|---|---|---|
| `ja` | 日本語 | 日本語 |
| `en` | 英語 | English |
| `fr` | フランス語 | Francais |
| `af` | アフリカーンス語 | Afrikaans |

ブラウザの言語設定を自動検出し、対応言語であればその言語で起動します。対応外の場合は日本語がデフォルトです。設定画面からいつでも変更できます。

### テーマ

| テーマ ID | 名称 | 特徴 |
|---|---|---|
| `white` | White | 明るい白背景。標準的なライトテーマ |
| `ivory` | Ivory | 温かみのあるクリーム色背景。長時間作業向き |
| `dark-blue` | Dark Blue | 深い青を基調としたダークテーマ |
| `black` | Black | 純粋な黒背景。OLED 対応 |

---

## 2. 文献ライブラリ

### 画面構成

- **ツールバー**: 表示切替（グリッド/リスト）、ソート選択、タグフィルタ、論文追加ボタン
- **グリッドビュー**: 論文を 3 カラムのカード形式で表示。タイトル、著者、年、タグ、読書ステータスバッジ
- **リストビュー**: 論文をテーブル形式で表示。ソート可能な列ヘッダー
- **詳細パネル**: 選択した論文のメタデータ全項目、PDF リーダー起動ボタン、ハイライト一覧、ノート一覧

### データモデル: Paper

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | UUID | 論文の一意識別子 |
| `title` | string | タイトル |
| `authors` | JSON array | 著者リスト |
| `year` | number? | 出版年 |
| `journal` | string? | ジャーナル名 |
| `volume` | string? | 巻号 |
| `issue` | string? | 号数 |
| `pages` | string? | ページ範囲 |
| `doi` | string? | DOI |
| `url` | string? | URL |
| `abstract` | string? | アブストラクト |
| `pdfPath` | string? | PDF ファイルのローカルパス |
| `tags` | JSON array | タグリスト |
| `readingStatus` | enum | `unread` / `reading` / `completed` / `rereading` |
| `createdAt` | ISO 8601 | 作成日時 |
| `updatedAt` | ISO 8601 | 更新日時 |

### 論文の追加方法

1. **DOI から追加**: DOI を入力 → CrossRef API でメタデータを自動取得
2. **URL から追加**: 論文ページの URL を入力 → Web スクレイピングでメタデータを抽出
3. **PDF から追加**: PDF ファイルを選択 → lopdf でメタデータ抽出を試行 → 手動補完
4. **手動追加**: 全フィールドを手入力
5. **ブラウザ拡張から追加**: ブラウザ拡張機能経由で論文ページから直接インポート

### Tauri コマンド（10 コマンド）

| コマンド | 説明 |
|---|---|
| `get_papers` | 論文一覧取得（ページネーション、フィルタ、ソート対応） |
| `get_paper` | 論文単体取得 |
| `create_paper` | 論文作成 |
| `update_paper` | 論文更新 |
| `delete_paper` | 論文削除（CASCADE: ハイライト・リンクも連動削除） |
| `attach_pdf` | PDF ファイルを論文に添付 |
| `extract_metadata_from_pdf` | PDF からメタデータを抽出 |
| `get_all_tags` | 全タグの一覧を取得 |
| `import_pdf` | PDF ファイルのインポート（ファイルダイアログ経由） |
| `save_note_attachment` | ノート添付ファイルの保存 |

---

## 3. PDF リーダー & ハイライト

### 画面構成

- **PDF ビューア**: pdfjs-dist ベースのレンダリング。ページスクロール、ズーム
- **ハイライトツールバー**: 4 色選択（黄 / 青 / 緑 / ピンク）、ハイライト削除
- **ハイライト一覧パネル**: 論文ごとのハイライトを時系列で表示。コメント編集、ノート自動生成

### ハイライト操作フロー

1. PDF 内のテキストを選択
2. ハイライト色を選択（キーボード `1`〜`4` または色ボタン）
3. ハイライトが保存される（テキスト、色、ページ番号、矩形座標）
4. オプション: ハイライトにコメントを追加
5. オプション: ハイライトからノートを自動生成

### データモデル: Highlight

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | UUID | ハイライトの一意識別子 |
| `paperId` | UUID | 所属する論文の ID |
| `text` | string | ハイライトされたテキスト |
| `comment` | string? | ユーザーコメント |
| `color` | string | 色コード（`#FFEB3B` / `#42A5F5` / `#66BB6A` / `#EF5350`） |
| `page` | number | ページ番号 |
| `rect` | JSON | 矩形座標（位置復元用） |
| `createdAt` | ISO 8601 | 作成日時 |

### Tauri コマンド（4 コマンド）

| コマンド | 説明 |
|---|---|
| `get_highlights` | 論文のハイライト一覧取得 |
| `create_highlight` | ハイライト作成 |
| `update_highlight_comment` | コメントの更新 |
| `delete_highlight` | ハイライト削除 |

---

## 4. ノート & エディタ

### 画面構成

- **ノート一覧**: タイトル、更新日時、紐付け論文、タグを表示。検索・フィルタ対応
- **エディタ**: CodeMirror 6 ベースの Markdown エディタ
- **コンテキストパネル**: アウトライン（見出し一覧）、バックリンク一覧

### エディタ機能

| 機能 | 説明 |
|---|---|
| **Markdown** | 見出し、太字、斜体、リスト、コードブロック、引用 |
| **WikiLink** | `[[ノート名]]` または `[[論文タイトル]]` で双方向リンクを作成 |
| **オートコンプリート** | `[[` 入力時にノート・論文の候補をリアルタイム検索・表示 |
| **アウトライン** | 見出し（H1〜H6）を自動抽出し、目次としてサイドに表示 |
| **バックリンク** | 当該ノートを `[[...]]` で参照している全ノート・論文を一覧表示 |
| **自動保存** | 入力停止から一定時間後に自動保存 |
| **フォーカスモード** | エディタを全画面化し集中環境を提供 |

### データモデル: Note

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | UUID | ノートの一意識別子 |
| `title` | string | タイトル |
| `content` | string | Markdown テキスト |
| `paperId` | UUID? | 紐付け論文の ID（独立ノートの場合は null） |
| `tags` | JSON array | タグリスト |
| `isDraft` | boolean | 下書きモードか |
| `draftMeta` | JSON? | 下書きメタデータ |
| `wordCount` | number | 語数 |
| `readingTimeMin` | number | 推定読了時間（分） |
| `createdAt` | ISO 8601 | 作成日時 |
| `updatedAt` | ISO 8601 | 更新日時 |

### Tauri コマンド（6 コマンド）

| コマンド | 説明 |
|---|---|
| `get_notes` | ノート一覧取得（ページネーション、フィルタ対応） |
| `get_note` | ノート単体取得 |
| `create_note` | ノート作成 |
| `update_note` | ノート更新 |
| `delete_note` | ノート削除 |
| `create_note_from_highlights` | ハイライト群からノートを自動生成 |

---

## 5. 下書きモード（長文執筆）

### 概要

論文・レポートなど長文を執筆するためのモードです。通常ノートを下書きモードに切り替えて使用します。

### 章管理

- **章の追加・削除・並び替え**: ドラッグ＆ドロップまたは矢印ボタン
- **章ごとの語数カウント**: リアルタイムで語数・推定読了時間を表示
- **章の展開・折りたたみ**: 長い文書でも効率的にナビゲーション

### 引用機能

- ライブラリの論文をインライン引用として挿入
- 4 つの引用スタイル対応: **APA 7th** / **MLA 9th** / **Chicago 17th** / **一橋スタイル**
- 著者名順序の設定（姓-名 / 名-姓）
- 参考文献リスト（Bibliography）の自動生成
- 引用一覧パネルで使用中の引用を一括管理

### Tauri コマンド（12 コマンド）

| コマンド | 説明 |
|---|---|
| `create_draft` | 下書き作成 |
| `get_drafts` | 下書き一覧取得 |
| `get_draft_chapters` | 章一覧取得 |
| `create_draft_chapter` | 章作成 |
| `update_draft_chapter` | 章更新 |
| `delete_draft_chapter` | 章削除 |
| `reorder_draft_chapters` | 章の並び替え |
| `insert_citation` | 引用挿入 |
| `get_citations_for_note` | ノート内の引用一覧取得 |
| `delete_citation` | 引用削除 |
| `generate_bibliography` | 参考文献リスト生成 |
| `sync_word_count` | 語数カウントの同期 |

---

## 6. グラフビュー（知識ネットワーク）

### 概要

論文とノートの関係をインタラクティブな力学グラフ（Force-Directed Graph）で可視化します。react-force-graph-2d を使用。

### ノードの種類

| 種類 | 色 | 表示内容 |
|---|---|---|
| 論文 | 青系 | タイトル |
| ノート | 緑系 | タイトル |

### インタラクション

| 操作 | 動作 |
|---|---|
| ノードホバー | ポップアップで詳細表示 |
| ノードクリック | 該当の論文/ノートを開く |
| ダブルクリック | ノードを中心にズーム |
| マウスホイール | ズームイン/アウト |
| ドラッグ（背景） | グラフ全体をパン |
| ドラッグ（ノード） | ノードを移動 |

### フィルタパネル

- **ノード種別**: 論文のみ / ノートのみ / 両方
- **タグフィルタ**: 特定タグを持つノードのみ表示
- **最小リンク数**: 指定数以上のリンクを持つノードのみ表示

### Tauri コマンド（4 コマンド）

| コマンド | 説明 |
|---|---|
| `get_graph_data` | グラフ用のノード・エッジデータ取得 |
| `create_link` | リンク作成 |
| `get_backlinks` | バックリンク取得 |
| `delete_link` | リンク削除 |

---

## 7. 引用ネットワーク

### 概要

論文間の引用・被引用関係を外部学術 API と連携して取得・可視化する機能です。

### 外部 API 連携

| API | 用途 |
|---|---|
| **Semantic Scholar** | 引用ネットワークの取得、関連論文レコメンデーション |
| **CrossRef** | DOI からのメタデータ取得 |
| **OpenAlex** | 補足的なメタデータ取得 |

### 機能一覧

- **引用ネットワーク取得**: 論文の参考文献・被引用文献を一括取得
- **引用グラフ可視化**: 引用関係を Force-Directed Graph で表示
- **関連論文レコメンデーション**: 関連度スコア付きの推薦論文リスト
- **ワンクリックインポート**: レコメンド論文をライブラリに即追加
- **読書ステータス管理**: 未読/読書中/完了/再読の 4 段階
- **エクスポート**: BibTeX / RIS 形式での引用情報出力

### Tauri コマンド（9 コマンド）

| コマンド | 説明 |
|---|---|
| `update_reading_status` | 読書ステータス更新 |
| `get_reading_status_counts` | ステータス別論文件数取得 |
| `fetch_citation_network` | 引用ネットワーク取得 |
| `fetch_recommendations` | 関連論文レコメンド取得 |
| `get_recommendations` | 保存済みレコメンド取得 |
| `import_recommendation` | レコメンド論文のインポート |
| `get_citation_graph_data` | 引用グラフデータ取得 |
| `export_bibtex` | BibTeX エクスポート |
| `export_ris` | RIS エクスポート |

---

## 8. 質的分析モジュール

### 概要

人文・社会科学の質的研究に必要な分析ツール群です。全 11 タブで構成され、55 の Tauri コマンドを提供する最大のモジュールです。

### 8.1 ダッシュボード

質的分析プロジェクトの概要を表示します。

- プロジェクトの作成・選択・削除
- コード数、ハイライト数、コーディング数などの集計表示
- プロジェクト横断的なサマリー

### 8.2 コードブック

階層型コードの作成と管理を行います。

- **ツリー構造**: 親コード・子コードの階層を構築
- **色分け**: 各コードに任意の色を設定
- **ドラッグ＆ドロップ**: コードの並び替え・階層変更
- **コードの割り当て**: ハイライトにコードを割り当て/解除

### 8.3 コーディングマトリクス

コード × 論文のクロス集計表を表示します。

- 各セルにコーディング件数を表示
- コーディングの偏りを俯瞰的に把握
- セルクリックで該当するハイライトを一覧表示

### 8.4 ICR（評価者間信頼性）

複数のコーダーによるコーディング結果の一致度を計算します。

- **Cohen's Kappa** の自動計算
- **一致率**（Percent Agreement）の表示
- 不一致セグメントの特定とリスト表示

### 8.5 史料批判シート

歴史学・文献学における史料批判を構造化して記録します。

| 項目 | 説明 |
|---|---|
| 著者情報 | 史料の作成者に関する情報 |
| 作成年代 | 史料が作成された時期 |
| 真正性 | 原本か写本か、偽作の可能性 |
| バイアス | 著者の立場や利害関係 |
| 信頼性スコア | 総合的な信頼度の評価 |

### 8.6 タイムライン

歴史的事象や研究対象のイベントを時系列で配置します。

- **複数レーン**: カテゴリごとにレーンを分けて表示
- **重要度設定**: イベントの重要度を 3 段階で設定
- **ズーム・スクロール**: 時間軸の拡大縮小

### 8.7 アクターマップ

政治的アクター（国家・組織・個人など）の関係をネットワーク図で可視化します。

- **アクターノード**: 名前、種別、説明
- **関係エッジ**: 関係性の種類（同盟 / 対立 / 従属など）
- **インタラクティブ操作**: ドラッグ、ズーム、ノード選択

### 8.8 プロセストレーシング

因果メカニズムの検証を支援する分析ツールです。

- **仮説設定**: 検証したい因果仮説を登録
- **証拠登録**: 4 種のテスト類型に基づく証拠の収集
  - **Hoop テスト**: 通過しなければ仮説を棄却
  - **Smoking Gun テスト**: 通過すれば仮説を強く支持
  - **Straw-in-the-Wind テスト**: 弱い支持/反証
  - **Doubly Decisive テスト**: 通過すれば確定的に支持
- **検証状況の管理**: 各証拠の結果（支持 / 反証 / 不明）を記録
- **サマリー**: 仮説ごとの検証状況の集計

### 8.9 比較分析

MSSD（最類似事例比較法）/ MDSD（最相違事例比較法）のデザインを支援します。

- **ケース × 変数マトリクス**: ケースと変数を軸とする表の構築
- **セル値の入力**: 各セルに値を記入
- **QCA CSV エクスポート**: QCA ソフトウェア用の CSV 出力

### 8.10 フレーミング分析

Entman のフレーミング理論に基づく分析を支援します。

- **フレーム定義**: 問題定義、因果解釈、道徳評価、対処勧告の 4 要素
- **ハイライトへの割り当て**: テキストハイライトにフレームを割り当て
- **フレーミングマトリクス**: フレーム × 論文のクロス集計

### 8.11 レポート

質的分析の結果をレポートとして出力します。

- プロジェクトの分析サマリー
- コーディング結果の集計
- 主要な知見の整理

### Tauri コマンド（55 コマンド）

プロジェクト管理 4 + コーディング 7 + マトリクス/ICR 2 + 史料批判 4 + タイムライン 5 + アクターマップ 5 + プロセストレーシング 5 + 比較分析 5 + フレーミング分析 4 + レポート 1 + エイリアス 5 + 追加 CRUD 8 = **55 コマンド**

---

## 9. 量的分析モジュール（Data Studio）

### 概要

CSV データの簡易的な統計分析を行う環境です。22 の Tauri コマンドを提供します。

### データセット管理

- **CSV インポート**: ファイル選択 → 自動パース → 変数型の自動検出
- **QDA 統合**: 質的分析のコードやハイライトからデータセットを自動生成
- **データプレビュー**: テーブル形式でデータを閲覧

### 変数管理

| 変数型 | 説明 |
|---|---|
| `scale` | 尺度（連続量） |
| `nominal` | 名義尺度（カテゴリ） |
| `ordinal` | 順序尺度 |
| `text` | テキスト |
| `date` | 日付 |

- リッカート尺度の設定（段階数、ラベル）
- 変数型の自動検出（`auto_detect_variable_types`）

### 分析ウィザード（3 ステップ）

1. **分析手法の選択**
2. **変数の指定**
3. **結果の表示**

### 分析手法

| 手法 | 説明 |
|---|---|
| **記述統計** | 平均、標準偏差、最小値、最大値、中央値、四分位範囲 |
| **頻度表** | カテゴリ変数の度数分布 |
| **相関分析** | ピアソン相関行列 |
| **t 検定** | 独立標本 t 検定 / Mann-Whitney U 検定 |
| **カイ二乗検定** | 独立性の検定 |
| **線形回帰** | 単回帰・重回帰 |

### テキストマイニング

- **トークン頻度**: テキスト変数のトークン頻度分析
- **TF-IDF**: 重要語の抽出

### チャート

- ヒストグラム
- 散布図
- 棒グラフ
- 箱ひげ図
- ワードクラウド
- ヒートマップ

### Tauri コマンド（22 コマンド）

データセット CRUD 5 + 変数 CRUD/検出 5 + データ行 3 + CSV インポート 1 + 分析 CRUD 4 + トークン頻度 2 + QDA 統合 2 = **22 コマンド**

---

## 10. エクスポート & パッケージ

### エクスポート形式

| 形式 | 説明 | コマンド |
|---|---|---|
| **静的サイト** | 選択したノートを HTML + CSS の静的サイトとして出力。テーマ選択、バックリンク含有オプション | `export_static_site` |
| **研究パッケージ** | 論文・ノート・ハイライト・リンク・PDF を `.stellar` ZIP ファイルにバンドル。別端末へのインポート対応 | `export_stellar_package` |
| **パッケージインポート** | `.stellar` ファイルから全データを復元。ID マッピング、コンフリクト処理付き | `import_stellar_package` |
| **JSON エクスポート** | 全データを JSON 形式で出力 | `export_data` |
| **DB バックアップ** | SQLite ファイル + WAL の直接コピー | `create_backup` |
| **BibTeX** | 引用情報を BibTeX 形式で出力 | `export_bibtex` |
| **RIS** | 引用情報を RIS 形式で出力 | `export_ris` |

### 研究パッケージの構造

```
package.stellar (ZIP)
├── manifest.json       — メタデータ（バージョン、件数、作成日時）
├── papers.json         — 論文データ
├── notes.json          — ノートデータ
├── highlights.json     — ハイライトデータ
├── links.json          — リンクデータ
└── pdfs/               — PDF ファイル（オプション）
    ├── <uuid>.pdf
    └── ...
```

---

## 11. クラウドバックアップ

### 設計原則

**金銭面以外のユーザー負担ゼロ**で PC 故障からデータを保護することを最優先に設計されています。

| 従来の手法 | Stellar の方式 |
|---|---|
| アカウント登録が必要 | **不要** — デバイス ID が自動生成される |
| パスワード設定が必要 | **不要** — リカバリーコードが自動導出される |
| バックアップ先の設定が必要 | **不要** — デフォルトでクラウドに保存 |
| 復元に複雑な手順が必要 | **リカバリーコード 12 桁を入力するだけ** |

### アーキテクチャ

```
┌────────────────────────────────────────────────────────────┐
│                     ユーザーの PC                           │
│                                                            │
│  ┌──────────┐    ┌──────────────┐    ┌─────────────────┐  │
│  │ SQLite DB │──→│ JSON シリアラ │──→│ ZIP 圧縮         │  │
│  │           │    │ イズ          │    │ (Deflate)       │  │
│  └──────────┘    └──────────────┘    └────────┬────────┘  │
│                                                │           │
│  ┌─────────────────────────────────────────────▼────────┐  │
│  │ AES-256-GCM 暗号化                                   │  │
│  │ 鍵: HMAC-SHA256(device_id + recovery_code)           │  │
│  │ Nonce: 12 バイトランダム値                             │  │
│  └─────────────────────────────────────────────┬────────┘  │
└────────────────────────────────────────────────┼───────────┘
                                                 │
                                    HTTPS (PUT)  │
                                                 ▼
                               ┌─────────────────────────┐
                               │ Cloudflare R2 Workers    │
                               │ stellar-backup.workers.  │
                               │ dev                      │
                               │                          │
                               │ 暗号文のみ保存            │
                               │ （復号不可能）             │
                               └─────────────────────────┘
```

### 暗号化フロー

1. **デバイス ID 生成**: `uuid::Uuid::new_v4()` で UUID v4 を生成
2. **リカバリーコード導出**: `HMAC-SHA256("stellar-recovery-code-v2", device_id)` → Base32-like エンコード → `XXXX-XXXX-XXXX` 形式（紛らわしい文字 0/O, 1/I/L を除外）
3. **暗号鍵導出**: `HMAC-SHA256("stellar-cloud-backup-v2", "stellar-backup-key:{device_id}:{recovery_code}")` → 256 ビット鍵
4. **暗号化**: AES-256-GCM（12 バイトランダム Nonce）
5. **転送**: HTTPS PUT で Cloudflare R2 Workers API にアップロード

### 暗号化済みバックアップの構造

```json
{
  "version": "2.0",
  "algorithm": "AES-256-GCM",
  "nonce": "<Base64>",
  "ciphertext": "<Base64>",
  "summary": {
    "paperCount": 42,
    "noteCount": 128,
    "highlightCount": 356,
    "linkCount": 89
  },
  "createdAt": "2026-05-08T12:34:56Z"
}
```

> `summary` は暗号化されません（一覧表示でデータ件数を表示するため）。実際のデータ内容は `ciphertext` 内に暗号化されています。

### リストアの挙動

| データ種別 | 既存データがある場合の挙動 |
|---|---|
| 論文 | `updated_at` を比較し、バックアップの方が新しければ上書き |
| ノート | `updated_at` を比較し、バックアップの方が新しければ上書き |
| ハイライト | 同一 ID が存在すればスキップ（`INSERT OR IGNORE`） |
| リンク | 同一 ID が存在すればスキップ（`INSERT OR IGNORE`） |

### ローカルフォールバック

ネットワーク未接続時やサーバーエラー時は、暗号化済みバックアップを `~/.stellar/cloud_backups/{backup_id}.enc` にローカル保存します。次回のバックアップ一覧取得時にローカルファイルも含めて表示されます。

### UI 操作フロー

#### 初回セットアップ

1. 設定画面 → データタブ → 「クラウドバックアップを有効にする」ボタンを押す
2. デバイス ID とリカバリーコードが自動生成される
3. **リカバリーコードを安全な場所に保管**（紙に書いてもよい）

#### 手動バックアップ

1. 「今すぐバックアップ」ボタンを押す
2. データ収集 → 暗号化 → アップロード（数秒〜十数秒）
3. バックアップ履歴に追加される

#### PC 故障時の復元

1. 新しい PC に Stellar をインストール
2. 設定画面 → データタブ → 「リカバリーコードで復元」を押す
3. 12 桁のリカバリーコード（`XXXX-XXXX-XXXX`）を入力
4. バックアップ一覧が表示される
5. 復元したいバックアップの「リストア」ボタンを押す
6. データがマージ方式でリストアされる

### Tauri コマンド（8 コマンド）

| コマンド | 説明 |
|---|---|
| `cloud_backup_setup` | 初回セットアップ（デバイス ID + リカバリーコード生成） |
| `cloud_backup_get_status` | バックアップ設定ステータス取得 |
| `cloud_backup_create` | バックアップ実行（暗号化 + アップロード） |
| `cloud_backup_list` | バックアップ一覧取得（クラウド + ローカル） |
| `cloud_backup_restore` | バックアップからリストア（復号 + DB マージ） |
| `cloud_backup_recover` | リカバリーコードでデバイス ID を復元（新端末用） |
| `cloud_backup_toggle_auto` | 自動バックアップの ON/OFF 切替 |
| `cloud_backup_set_api_url` | バックアップ API URL の変更（セルフホスト対応） |

---

## 12. ブラウザ連携

### 概要

Stellar は `127.0.0.1:57321` で HTTP サーバーを起動し、ブラウザ拡張機能からの論文インポートを受け付けます。

### エンドポイント

| メソッド | パス | 説明 |
|---|---|---|
| `GET` | `/api/status` | サーバーステータス確認 |
| `POST` | `/api/import` | 論文メタデータのインポート |
| `OPTIONS` | `*` | CORS プリフライト対応 |

### インポートフロー

1. ユーザーがブラウザで論文ページを閲覧
2. ブラウザ拡張機能のボタンを押す
3. 拡張機能がページからメタデータ（タイトル、著者、DOI、URL）を抽出
4. `POST /api/import` で Stellar に送信
5. Stellar がデータを DB に保存し、ライブラリに追加

### 設定画面での表示

- サーバーステータスバッジ（Running / Waiting）
- インストールガイド（4 ステップ）
- 最近のインポート履歴（直近 5 件）

---

## 13. 設定 & カスタマイズ

### 外観タブ

| 設定項目 | 範囲 | デフォルト |
|---|---|---|
| テーマ | White / Ivory / Dark Blue / Black | Ivory |
| フォントサイズ | 13〜16 px | 14 px |
| 行高さ | 1.5〜2.0 | 1.6 |
| エディタフォント | System / Noto Sans JP / Noto Serif JP / Source Code Pro / JetBrains Mono / IBM Plex Mono | System |

### データタブ

- **データサマリー**: 論文数、ノート数、ハイライト数、ディスク使用量
- **データ保存先**: データベースファイルのパスを変更可能
- **エクスポート**: JSON エクスポート、DB バックアップ
- **研究パッケージ**: `.stellar` パッケージのエクスポート/インポート
- **ブラウザ連携**: サーバーステータス、インストールガイド、最近のインポート
- **クラウドバックアップ**: セットアップ、手動実行、履歴、リストア、リカバリーコード管理、自動バックアップ設定

### ショートカットタブ

ナビゲーション / エディタ / グラフ / PDF リーダーの 4 カテゴリでショートカット一覧を表示します（詳細は[18 章](#18-キーボードショートカット一覧)を参照）。

### 引用スタイルタブ

| スタイル | 説明 |
|---|---|
| APA 7th | アメリカ心理学会スタイル第 7 版 |
| MLA 9th | アメリカ現代語学協会スタイル第 9 版 |
| Chicago 17th | シカゴ大学出版局スタイル第 17 版 |
| 一橋スタイル | 一橋大学の引用スタイル |

著者名の順序設定: **姓-名** / **名-姓**

### 言語タブ

UI 言語の切り替え（日本語 / English / Francais / Afrikaans）。

---

## 14. オンボーディング

初回起動時に表示される 5 ステップのウィザードです。

| ステップ | 内容 |
|---|---|
| 1 | ウェルカム画面 — Stellar の紹介 |
| 2 | 言語選択 — 4 言語から選択 |
| 3 | データ保存先 — データベースファイルの保存場所を設定 |
| 4 | テーマ選択 — 4 テーマのプレビューと選択 |
| 5 | セットアップ完了 — 使い方のヒント表示 |

---

## 15. 全文検索

### 技術仕様

| 項目 | 仕様 |
|---|---|
| エンジン | SQLite FTS5 |
| トークナイザ | trigram（3 文字分割） |
| 対応言語 | 日本語を含む全言語（部分一致対応） |
| 検索対象 | 論文（タイトル、著者、アブストラクト）、ノート（タイトル、内容）、ハイライト（テキスト） |

### FTS テーブル

| テーブル | 対象 | トリガー |
|---|---|---|
| `fts_search` | 論文 + ノート | INSERT / UPDATE / DELETE に連動 |
| `fts_highlights` | ハイライト | INSERT / UPDATE / DELETE に連動 |

### Tauri コマンド（3 コマンド）

| コマンド | 説明 |
|---|---|
| `full_text_search` | 全文検索（論文 + ノート + ハイライト横断） |
| `get_link_suggestions` | WikiLink オートコンプリート候補の検索 |
| `get_recent_items` | 最近のアイテム取得（更新順） |

---

## 16. データベース設計

### 概要

| 項目 | 仕様 |
|---|---|
| DBMS | SQLite 3 |
| ジャーナルモード | WAL（Write-Ahead Logging） |
| マイグレーション | V001〜V006（6 バージョン、`sqlx` で管理） |
| テーブル総数 | 約 30 テーブル |
| FTS インデックス | 2 テーブル（trigram トークナイザ） |
| トリガー | FTS 同期、updated_at 自動更新、row_count 集計 |

### 整合性の保証

- **外部キー制約**: `PRAGMA foreign_keys = ON`
- **CASCADE DELETE**: 論文削除時にハイライト・リンクを連動削除
- **SET NULL**: 論文削除時にノートの紐付けを解除
- **ユニーク制約**: リンクの重複防止（source + target の組み合わせ）
- **INSERT OR IGNORE**: ハイライト・リンクの重複インポート防止

---

## 17. セキュリティ仕様

### Tauri CSP（Content Security Policy）

```
default-src   : 'self'
script-src    : 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://esm.sh
style-src     : 'self' 'unsafe-inline' https://cdn.jsdelivr.net
img-src       : 'self' asset: https://asset.localhost blob: data:
font-src      : 'self' data:
connect-src   : 'self' ipc: http://ipc.localhost asset: https://asset.localhost blob:
                https://api.crossref.org https://api.openalex.org
                https://cdn.jsdelivr.net https://esm.sh
                https://stellar-backup.workers.dev
worker-src    : 'self' blob:
```

### Tauri Capabilities

ファイルシステムアクセスは Tauri の Capability 機構で制限されています。

| 権限 | 範囲 |
|---|---|
| `fs:allow-app-*` | アプリ設定ディレクトリのみ |
| `fs:allow-appdata-*` | アプリデータディレクトリのみ |
| `fs:allow-home-*` | ホームディレクトリ |
| `fs:allow-document-*` | ドキュメントディレクトリ |
| `fs:allow-download-*` | ダウンロードディレクトリ |
| `fs:allow-desktop-*` | デスクトップディレクトリ |

### クラウドバックアップの暗号化仕様

| 項目 | 詳細 |
|---|---|
| 暗号化アルゴリズム | AES-256-GCM（認証付き暗号化） |
| 鍵長 | 256 ビット |
| Nonce 長 | 96 ビット（12 バイト） |
| 鍵導出関数 | HMAC-SHA256 |
| 鍵素材 | `"stellar-backup-key:{device_id}:{recovery_code}"` |
| HMAC 鍵 | `"stellar-cloud-backup-v2"` |
| リカバリーコード | 12 桁、Base32-like（紛らわしい文字を除外）、`XXXX-XXXX-XXXX` 形式 |
| E2E 暗号化 | サーバーは暗号文のみを保管。復号鍵はクライアントにのみ存在 |

---

## 18. キーボードショートカット一覧

### ナビゲーション

| ショートカット | 動作 |
|---|---|
| `Ctrl/Cmd + K` | 検索を開く |
| `Ctrl/Cmd + N` | 新規ノート作成 |
| `Ctrl/Cmd + ,` | 設定を開く |
| `Ctrl/Cmd + 1` | ライブラリに切り替え |
| `Ctrl/Cmd + 2` | ノートに切り替え |
| `Ctrl/Cmd + 3` | グラフビューに切り替え |

### エディタ

| ショートカット | 動作 |
|---|---|
| `Ctrl/Cmd + S` | 保存 |
| `Ctrl/Cmd + B` | 太字 |
| `Ctrl/Cmd + I` | 斜体 |
| `Ctrl/Cmd + Z` | 元に戻す |
| `Ctrl/Cmd + Shift + Z` | やり直す |
| `[[` | WikiLink 挿入（オートコンプリート起動） |

### グラフビュー

| ショートカット | 動作 |
|---|---|
| `Cmd/Ctrl + A` | 全選択 |
| マウスホイール | ズーム |
| ドラッグ（背景） | パン |
| ダブルクリック | ノードを開く |

### PDF リーダー

| ショートカット | 動作 |
|---|---|
| `←` / `→` | 前ページ / 次ページ |
| `Ctrl/Cmd + +` | ズームイン |
| `Ctrl/Cmd + -` | ズームアウト |
| `Ctrl/Cmd + 0` | ズームリセット |
| `Ctrl/Cmd + F` | テキスト検索 |
| `1`〜`4` | ハイライト色の選択 |

---

## 付録: Tauri コマンド一覧（モジュール別）

| モジュール | コマンド数 | 主な機能 |
|---|---|---|
| `papers.rs` | 10 | 論文 CRUD、PDF インポート、タグ管理 |
| `notes.rs` | 6 | ノート CRUD、ハイライトからのノート生成 |
| `highlights.rs` | 4 | ハイライト CRUD |
| `links.rs` | 4 | リンク CRUD、グラフデータ取得 |
| `search.rs` | 3 | 全文検索、WikiLink 候補、最近のアイテム |
| `metadata.rs` | 4 | CrossRef/OpenAlex メタデータ取得、PDF ダウンロード、書誌情報フォーマット |
| `citation_network.rs` | 9 | 引用ネットワーク、レコメンド、BibTeX/RIS エクスポート |
| `qualitative.rs` | 55 | プロジェクト管理、コーディング、マトリクス、ICR、史料批判、タイムライン、アクターマップ、プロセストレーシング、比較分析、フレーミング分析、レポート |
| `quantitative.rs` | 22 | データセット CRUD、変数管理、CSV インポート、統計分析、テキストマイニング、QDA 統合 |
| `draft.rs` | 12 | 下書き CRUD、章管理、引用管理、参考文献リスト生成 |
| `export.rs` | 3 | 静的サイトエクスポート、研究パッケージのエクスポート/インポート |
| `data.rs` | 7 | データサマリー、ディスク使用量、パス管理、JSON エクスポート、DB バックアップ |
| `cloud_backup.rs` | 8 | クラウドバックアップのセットアップ、実行、一覧、リストア、リカバリー、自動バックアップ、API URL 設定 |
| **合計** | **147** | |

---

## 付録: コード規模

| | ファイル数 | 行数 |
|---|---|---|
| **フロントエンド**（TypeScript / TSX） | 132 | 約 56,700 行 |
| **バックエンド**（Rust） | 24 | 約 14,700 行 |
| **合計** | 156 | 約 71,400 行 |

---

## 付録: 関連ドキュメント

| ドキュメント | 対象読者 | 内容 |
|---|---|---|
| **[README.md](README.md)** | 開発者・コントリビューター | アーキテクチャ概要、技術スタック、セットアップ手順 |
| **FEATURES.md**（本文書） | 開発者・PM | 全 147 コマンドの詳細リファレンス、データモデル、セキュリティ仕様 |
| **[Stellar ユーザーガイド](Stellar_ユーザーガイド.md)** | エンドユーザー（初心者〜上級者） | 画面操作、チュートリアル、FAQ |
