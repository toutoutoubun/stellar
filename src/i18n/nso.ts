// Sepedi (Northern Sotho) translations
// Pending review: currently falls back to English strings.

import type { TranslationKeys } from "./ja";
import en from "./en";

const nso: TranslationKeys = en;

export default nso;
